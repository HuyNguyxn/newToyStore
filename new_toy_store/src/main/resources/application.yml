spring:
  application:
    name: new_toy_store

  datasource:
    url: jdbc:mysql://localhost:3306/new_toy_store
    username: root
    password: Huynguyen_07

  flyway:
    enabled: true
    locations: classpath:db/migration
    detect-encoding: true
    baseline-on-migrate: false

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
        jdbc:
          batch_size: 50
        order_inserts: true
        order_updates: true

  mail:
    host: smtp.gmail.com
    port: 587
    username: frocoding8888@gmail.com
    password: izsl leeg scsg novm
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

app:
  seed:
    admin:
      enabled: ${SEED_ADMIN_ENABLED:true}
      email: ${SEED_ADMIN_EMAIL:admin@gmail.com}
      password: ${SEED_ADMIN_PASSWORD:123456}
      full-name: ${SEED_ADMIN_FULL_NAME:Admin}
      phone-number: ${SEED_ADMIN_PHONE:0900000001}
    staff:
      enabled: ${SEED_STAFF_ENABLED:true}
      email: ${SEED_STAFF_EMAIL:staff@gmail.com}
      password: ${SEED_STAFF_PASSWORD:123456}
      full-name: ${SEED_STAFF_FULL_NAME:Staff}
      phone-number: ${SEED_STAFF_PHONE:0900000002}
    customer:
      enabled: ${SEED_CUSTOMER_ENABLED:true}
      email: ${SEED_CUSTOMER_EMAIL:customer@gmail.com}
      password: ${SEED_CUSTOMER_PASSWORD:123456}
      full-name: ${SEED_CUSTOMER_FULL_NAME:Customer}
      phone-number: ${SEED_CUSTOMER_PHONE:0900000003}
  cart:
    cleanup:
      cron: "0 0 0 * * ?"
      zone: "Asia/Ho_Chi_Minh"
  order:
    timeout:
      cron: "0 */15 * * * *"
      zone: "Asia/Ho_Chi_Minh"
      expiration-minutes: 30
  supplier-return:
    sla:
      cron: "0 0 8 * * ?"
      zone: "Asia/Ho_Chi_Minh"
      warning-hours: 48
      critical-hours: 96
  payment:
    vnpay:
      enabled: true
      pay-url: "https://sandbox.vnpayment.vn/paymentv2/vpcpay.html"
      refund-url: "https://sandbox.vnpayment.vn/merchant_webapi/api/transaction"
      tmn-code: "${VNPAY_TMN_CODE:K0FNJ3HC}"
      hash-secret: "${VNPAY_HASH_SECRET:CQYTEUDFQWEVLEYFNYWGQQLPHJTPRYXN}"
      return-url: "${VNPAY_RETURN_URL:http://localhost:8080/payments/vnpay-return}"
      ipn-url: "${VNPAY_IPN_URL:http://localhost:8080/payments/vnpay-ipn}"
  notification:
    zone: "Asia/Ho_Chi_Minh"
    retention-days: 90
    cleanup:
      cron: "0 30 2 * * ?"
  cloudinary:
    cloud-name: ${CLOUDINARY_CLOUD_NAME:}
    api-key: ${CLOUDINARY_API_KEY:}
    api-secret: ${CLOUDINARY_API_SECRET:}
    folder: ${CLOUDINARY_FOLDER:new-toy-store}
